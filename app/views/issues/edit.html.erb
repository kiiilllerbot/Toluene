<% content_for :title do %>
  Edit Issue <%= @issue.title %>
<% end %>
<% provide(:issue_active, 'active') %>
<div class="project-new-cover"></div>
<div class="container-fluid">
	<div class="row justify-content-center">
		<div class="col-sm-12 col-md-10 col-lg-10" style="margin-top: 14vh">
			<div class="card">
				<div class="card-body">
					<h3 class="pb-1"><i class="fas fa-folder-plus"></i> Edit Issue <strong><%= @issue.title %></strong></h3>
					<%= render 'form', issue: @issue %>
				</div>
			</div>
		</div>
	</div>
</div>